<Window x:Class="Isoline.SettingsWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:local="clr-namespace:Isoline"
		xmlns:util="clr-namespace:Isoline.Util"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
		mc:Ignorable="d"
		Title="SettingsWindow" Height="380" Width="420" Closed="Window_Closed" ResizeMode="NoResize" Background="{DynamicResource SurfaceRaisedBrush}" Foreground="{DynamicResource TextBrush}">
	<Window.Resources>
		<Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
			<Setter Property="VerticalContentAlignment" Value="Center"/>
			<Setter Property="HorizontalContentAlignment" Value="Right"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
			<Setter Property="VerticalAlignment" Value="Center"/>
			<Setter Property="Height" Value="24"/>
			<Setter Property="Width" Value="120"/>
		</Style>
	</Window.Resources>
	<TabControl Margin="0" BorderThickness="0" Background="Transparent">
		<TabItem Header="Connection">
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<StackPanel Margin="10,15,10,10">
					<!-- Connection Type -->
					<Grid Margin="0,0,0,12">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="135"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<ComboBox x:Name="comboBoxConnectionType" Width="120" HorizontalAlignment="Left" VerticalAlignment="Center" SelectedValue="{util:SettingBinding ConnectionType}"/>
						<Label Grid.Column="1" Content="Connection Type" VerticalAlignment="Center" Margin="0"/>
					</Grid>

					<!-- Serial Settings -->
					<Grid Margin="0,0,0,12">
						<Grid.Style>
							<Style TargetType="Grid">
								<Setter Property="Visibility" Value="Collapsed"/>
								<Style.Triggers>
									<DataTrigger Binding="{util:SettingBinding ConnectionType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="Serial">
										<Setter Property="Visibility" Value="Visible"/>
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Grid.Style>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="135"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<ComboBox x:Name="ComboBoxSerialPort" Width="120" HorizontalAlignment="Left" VerticalAlignment="Center" SelectedValuePath="Tag" SelectedValue="{util:SettingBinding SerialPortName}" DropDownOpened="ComboBoxSerialPort_DropDownOpened"/>
						<Label Grid.Column="1" Content="Serial Port" VerticalAlignment="Center" Margin="0"/>

						<TextBox Grid.Row="2" Text="{util:SettingBinding SerialPortBaud}" Width="120" HorizontalAlignment="Left"/>
						<Label Grid.Row="2" Grid.Column="1" Content="BaudRate" VerticalAlignment="Center" Margin="0"/>
					</Grid>

					<!-- Ethernet Settings -->
					<Grid Margin="0,0,0,12">
						<Grid.Style>
							<Style TargetType="Grid">
								<Setter Property="Visibility" Value="Collapsed"/>
								<Style.Triggers>
									<DataTrigger Binding="{util:SettingBinding ConnectionType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Value="Ethernet">
										<Setter Property="Visibility" Value="Visible"/>
									</DataTrigger>
								</Style.Triggers>
							</Style>
						</Grid.Style>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="135"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<TextBox Text="{util:SettingBinding EthernetIP}" Width="120" HorizontalAlignment="Left"/>
						<Label Grid.Column="1" Content="IP" VerticalAlignment="Center" Margin="0"/>

						<TextBox Grid.Row="2" Text="{util:SettingBinding EthernetPort}" Width="120" HorizontalAlignment="Left"/>
						<Label Grid.Row="2" Grid.Column="1" Content="Port" VerticalAlignment="Center" Margin="0"/>
					</Grid>

					<!-- Other settings -->
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="135"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="12"/>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="10"/>
						</Grid.RowDefinitions>

						<CheckBox Grid.Row="0" Grid.Column="1" Content="Reset grbl when connecting" IsChecked="{util:SettingBinding SerialPortDTR}" ToolTip="the DTR signal is used to reset grbl when the connection is established, set by default on many other gcode senders"/>

						<TextBox Grid.Row="2" Text="{util:SettingBinding ControllerBufferSize}" Width="120" HorizontalAlignment="Left"/>
						<Label Grid.Row="2" Grid.Column="1" Content="Controller Buffer" VerticalAlignment="Center" Margin="0"/>

						<TextBox Grid.Row="4" Text="{util:SettingBinding StatusPollInterval}" Width="120" HorizontalAlignment="Left"/>
						<Label Grid.Row="4" Grid.Column="1" Content="Status Poll Interval (ms)" VerticalAlignment="Center" Margin="0"/>

						<CheckBox Grid.Row="6" Grid.Column="1" Content="Log traffic to file" IsChecked="{util:SettingBinding LogTraffic}" ToolTip="Restart required to take effect"/>
						
						<CheckBox Grid.Row="8" Grid.Column="1" Content="Ignore Additional Axes in Status Messages" IsChecked="{util:SettingBinding IgnoreAdditionalAxes}" ToolTip="Compatibility with custom GRBL versions which report 4+ axes in status reports"/>

						<ComboBox Grid.Row="10" x:Name="FirmwareType" Width="120" HorizontalAlignment="Left" VerticalAlignment="Center" SelectedItem="{util:SettingBinding FirmwareType}" SelectionChanged="FirmwareType_SelectionChanged">
							<sys:String>Grbl</sys:String>
							<sys:String>uCNC</sys:String>
						</ComboBox>
						<Label Grid.Row="10" Grid.Column="1" Content="Board firmware" VerticalAlignment="Center" Margin="0"/>
					</Grid>
				</StackPanel>
			</ScrollViewer>
		</TabItem>

		<TabItem Header="Viewport">
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<Grid Margin="10,15,10,10">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="135"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<TextBox Grid.Row="0" Text="{util:SettingBinding ViewportArcSplit}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="0" Grid.Column="1" Content="preview arc segment length (mm)" VerticalAlignment="Center" Margin="0"/>

					<TextBox Grid.Row="2" Text="{util:SettingBinding HeightMapOpacity}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="2" Grid.Column="1" Content="height map opacity (0.0 - 1.0)" VerticalAlignment="Center" Margin="0"/>

					<TextBox Grid.Row="4" Text="{util:SettingBinding GridThickness}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="4" Grid.Column="1" Content="grid thickness (0.1 default)" VerticalAlignment="Center" Margin="0"/>

					<CheckBox Grid.Row="6" Grid.Column="1" Content="Preview Toolpath" IsChecked="{util:SettingBinding EnableCodePreview}"/>
					<CheckBox Grid.Row="8" Grid.Column="1" Content="Send Soft Reset on Escape Key press" IsChecked="{util:SettingBinding EnableEscapeSoftReset}"/>
				</Grid>
			</ScrollViewer>
		</TabItem>

		<TabItem Header="Probing">
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<Grid Margin="10,15,10,10">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="135"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="4"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="12"/>
						<RowDefinition Height="Auto"/>
					</Grid.RowDefinitions>

					<TextBox Grid.Row="0" Text="{util:SettingBinding ProbeFeed}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="0" Grid.Column="1" Content="Probe Plunge Feedrate (mm/min)" VerticalAlignment="Center" Margin="0"/>

					<CheckBox Grid.Row="2" Grid.Column="1" Content="Abort on Probe Fail" IsChecked="{util:SettingBinding AbortOnProbeFail}"/>

					<TextBox Grid.Row="4" Text="{util:SettingBinding ProbeSafeHeight}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="4" Grid.Column="1" Content="Retraction height for large moves (mm)" VerticalAlignment="Center" Margin="0"/>

					<TextBox Grid.Row="6" Text="{util:SettingBinding ProbeMinimumHeight}" ToolTip="This height will be added to the height of the last probe point to prevent crashes on highly tilted surfaces" Width="120" HorizontalAlignment="Left"/>
					<TextBlock Grid.Row="6" Grid.RowSpan="3" Grid.Column="1" Text="Retraction height for short moves (mm)&#x0a;(the height of the highest neighbouring point is added to this height)" VerticalAlignment="Center" TextWrapping="Wrap" Margin="5,0,0,0"/>

					<TextBox Grid.Row="10" Text="{util:SettingBinding ProbeMaxDepth}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="10" Grid.Column="1" Content="Max Depth (mm)" VerticalAlignment="Center" Margin="0"/>

					<TextBox Grid.Row="12" Text="{util:SettingBinding ProbeXAxisWeight}" Width="120" HorizontalAlignment="Left"/>
					<Label Grid.Row="12" Grid.Column="1" Content="Probe X-Axis Weight" VerticalAlignment="Center" ToolTip="Use this value to tell Isoline to prefer to move X or Y preferably when probing. This is useful if one of both axes is lighter or faster than the other. Values smaller or greater than 1.0 change the preferred axis, 1.0 means no preferred axis." Margin="0"/>

					<CheckBox Grid.Row="14" Grid.Column="1" Content="Auto-Backup HeightMap when complete" IsChecked="{util:SettingBinding BackupHeightMap}" ToolTip="Backups are stored in the same folder as Isoline.exe"/>
				</Grid>
			</ScrollViewer>
		</TabItem>

		<TabItem Header="GCode">
			<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
				<StackPanel Margin="10,15,10,10" Orientation="Vertical">
					<CheckBox Content="Keep Spindle 'S' Commands" IsChecked="{util:SettingBinding GCodeIncludeSpindle}" ToolTip="Strip S[XX] from file before sending to machine if not checked" Margin="135,0,0,12"/>
					<CheckBox Content="Keep Dwell 'G4' Commands" IsChecked="{util:SettingBinding GCodeIncludeDwell}" ToolTip="Strip G4 P[XX] from file before sending to machine if not checked" Margin="135,0,0,12"/>
					<CheckBox Content="Keep Program End (M2, M30)" IsChecked="{util:SettingBinding GCodeIncludeMEnd}" ToolTip="Strip M2/M30 from file before sending to machine if not checked. Default is off since these commands reset offsets from G92." Margin="135,0,0,0"/>
				</StackPanel>
			</ScrollViewer>
		</TabItem>
	</TabControl>
</Window>
